// Mock the props
export const navigation: any = {
    navigate: jest.fn(),
    goBack: jest.fn(),
    dispatch: jest.fn(),
    addListener: jest.fn(),
    // Add any other required properties or methods
};

export const route: any = {
    params: {},
    // Add any other required properties or methods
};
